Ideally, both server and client would draw from this as a source of truth for our types.

In a real setting, this would be structured in such a way that our schema and types stay in sync, without exposing schema data to client.
